import { Component, OnInit } from '@angular/core';  // Importa la clase Component desde Angular para definir un componente.

@Component({
  selector: 'app-header',  // Define el nombre del selector del componente, que se usará para incluir el componente en las plantillas HTML.
  templateUrl: './header.component.html',  // Especifica el archivo HTML que define la estructura del componente.
  styleUrls: ['./header.component.css']  // Especifica el archivo CSS que define los estilos del componente.
})
export class HeaderComponent {
  // La clase del componente "HeaderComponent" no tiene lógica o propiedades adicionales por el momento.
}


