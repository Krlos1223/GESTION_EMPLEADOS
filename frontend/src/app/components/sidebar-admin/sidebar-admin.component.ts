import { Component } from '@angular/core'; // Importa el decorador Component para definir el componente

@Component({
  selector: 'app-sidebar-admin',  // Nombre del selector del componente que se usará en la plantilla
  templateUrl: './sidebar-admin.component.html',  // Ruta del archivo de plantilla HTML asociado
  styleUrls: ['./sidebar-admin.component.css']  // Ruta del archivo de estilos CSS asociado
})
export class SidebarAdminComponent {
  // Este componente actualmente no tiene lógica o propiedades definidas
}